<% provide(:title, 'Edit Rent Roll') %>

<% if current_user.admin? %>
  <h1><%= yield(:title) %></h1><hr>

<%= render 'form' %>

<%= link_to 'Show', @roll %> |
<%= link_to 'Back', rolls_path %>
<% else %>

        <div class="row">
      <div class="span12">
          <div class="well">
            <center>
            <h1>Oops! This page doesn't exist!</h1>
            </center>
          </div>
      </div>
    </div>

<% end %>
