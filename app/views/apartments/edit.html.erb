<% if current_user.admin? %>

	<h1>Editing apartment - <%= @apartment.streetAddress %>, #<%= @apartment.aptNumber %></h1>

	<%= render 'form' %>

<% else %>
  <div class="row">
      <div class="span12">
          <div class="well">
            <center>
            <h1>Oops! This unit is currently unavailable</h1>
            </center>
          </div>
      </div>
  </div>
<% end %>